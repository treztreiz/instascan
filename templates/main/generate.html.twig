{% extends 'base.html.twig' %}

{% block body %}

  {{form_start(form)}}
  {{form_widget(form)}}
  <div class="text-end"><button type="submit" class="btn btn-lg btn-primary">générer</button></div>
  {{form_end(form)}}

{% endblock %}